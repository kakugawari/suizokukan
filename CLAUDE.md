# このリポジトリと、アプリの作り方

<!-- ここから: 新しいアプリを始めたら、この節を書き換える -->

## 何のアプリか

**すいぞくかん** —— 魚を合わせて集める、放置型の水族館アプリ。

- 水槽の魚を**ドラッグして重ねる**と合体する
  - 同じ魚どうし → 色ちがい(ふつう / 色A / 色B / しろ / きん)が抽選で生まれる
  - なかよしの組 (`BREED`) → 別の魚が生まれる
  - 魚をタップすると +1 円
- **ショップ**でおきものを買って置くと、決まった魚が出てくる(水槽は最大 18 ひき)
- 開いていない間も**エサ(お金)がたまる**(最大 8 時間ぶん)。
  「エサやり」のタイミングゲーム 5 回で受けとる
- **ずかん**: 10 種 × 5 すがた = 50。数がそろうとごほうび

もとは Artifact で作ったプロトタイプ (1 枚の HTML) を、この形に分けたもの。
保存は `localStorage` の `suizokukan_v2`。

<!-- ここまで -->

---

## 動かす・試す

```bash
npm start          # http://localhost:8080/ で開く
npm test           # ロジックのテスト (node だけで動く。速い)
npm run test:ui    # ブラウザで実際に動かすテスト (要 npm i -D playwright)
```

`npm run test:ui` は本物のブラウザを立ち上げ、指の操作をそのまま再現する。
**画面を触ったときの不具合はこちらでしか捕まらない。** 変更したら必ず両方流す。

## 構成

```
index.html / styles.css   画面
core.js                   ロジック。DOM を触らない。ブラウザと node の両方で動く
app.js                    操作・描画
img/                      絵 (WebP)。いきもの f_*・おきもの d_*・水槽の背景 tank
core.test.js              ロジックのテスト (node --test)
browser-test.js           ブラウザで動かすテスト
serve.js                  開発用サーバー
```

ファイルは絵 (`img/`) 以外すべて直下に置く。スマホやタブレットからでも
1 ファイルずつ差し替えられるようにするため。増えてきたら
`src/` などに分けてよい (そのときは上の表と読み込み先も直す)。

絵を足したら `core.js` の `IMG_SIZE` に元の大きさ [幅, 高さ] も足す
(縦横比に使う。足し忘れは `npm test` が見つける)。

`window.__app` はテストから中身をのぞくための入口。`browser-test.js` が使う。

## 決めごと

- **対象の端末は iPhone 16 Plus だけ。** ほかの画面幅は見ない。
  幅に応じて詰める書き方 (`min(62px, 16vw)` など) も、
  そのための見張りも要らない。寸法は実機の 430pt で決め打ってよい
- 入っている playwright が `iPhone 16 Plus` を知らないときは、
  同じ 430 幅の `iPhone 15 Plus` で代えてテストする

## まだ直していないこと (プロトタイプから持ちこんだまま)

- 根っこの高さが `height: 100%` のまま。落とし穴の表のとおり、ホーム画面から
  開くと下に帯が出るおそれがある (`100vh` + `position: relative` にする)
- 魚を動かすたび・3 秒ごとに `localStorage` へ同期で書きこんでいる
- ホーム画面用のアイコン (PNG)・サービスワーカーはまだ無い

---

## 作り方(毎回これをなぞる)

### 1. 土台を決める

- **正しさを仕組みで保証できないか、先に考える。** 「あとで検算する」より
  「そもそも間違いようがない作り」の方が強い
  (例: パズルなら、答えから逆算して問題を作れば必ず解ける)
- **ロジックを画面から切り離す。** `src/core.js` は DOM を触らない。
  だから node でテストできる。テストできる形にしておくと、あとが全部楽になる
- **ビルドを挟まない。** 素の HTML / CSS / JS。動かすまでの手数が少ないほど、
  確かめる回数が増える

### 2. 確かめる

- **「テストが通った」で終わらせない。実際に動かして目で見る。**
  画面の不具合はユニットテストをすり抜ける
- **体感の問題は計測する。** 「なんか遅い気がする」に対しては、
  1 フレームずつ位置や時間を記録して数字を出す。憶測で直さない
- **端やずらした条件でも試す。** 原点(0,0)や初期状態だけで試すと、
  ずれていても差が出ず見逃す
- **直したら見張りのテストを足す。** 同じ失敗を二度しないための唯一の方法
- **スマホの幅と、遅い端末でも見る。** CPU を 4 倍遅くして計測すると差が見える

### 3. テストプレイしてもらう

1. 全ファイルを 1 枚の HTML にまとめる(CSS も JS も埋め込む)
2. Artifact として公開し、リンクを渡す。スマホでもすぐ開ける
3. **そのリンクは Claude 側の仮ページ。** アドレスもタブのアイコンも Claude のものになる。
   本番のアドレスとは別だと必ず伝える
4. 直したら同じリンクを更新する(新しいリンクを作らない)

### 4. git と公開

- **作業用ブランチで進める。main へは、言われてから出す**
- コミットは「何を、なぜ」。直した不具合は**原因**まで書く。日本語でよい
- **push する前に `npm test` と `npm run test:ui` を両方流す**
- マージやデプロイの前に、**実際の設定を確認してから話す。**
  ブランチ一覧と CI の実行履歴を見れば分かることを、憶測で説明しない

### 5. 報告のしかた

- 直したことは事実で書く。**計測値や試した件数を添える**
- できていないこと、飛ばしたことは、そのまま言う
- 間違えたら、言い訳せずその場で訂正して先へ進む

---

## 踏んだ落とし穴(同じ轍を踏まないために)

画面まわりは、動いているように見えて壊れていることがある。実際に踏んだもの:

| 症状 | 原因 | 直し方 |
| --- | --- | --- |
| 置いた直後に要素が一瞬とぶ | CSS アニメーションの `transform: scale` が、位置決めのインライン `transform: translate` を上書きしていた | 位置は `left`/`top` で決める |
| アニメーションが 1 フレームも動かない | 同じフレーム内で書き換えると、ブラウザは途中の状態を見ない | `element.animate()` (Web Animations API) を使う |
| まわすと指の下から対象が逃げる | 回転で縦横が入れ替わり、並び全体がずれていた | 入れ物を正方形にして、回しても大きさを変えない |
| ホーム画面のアイコンが違うものになる | iOS は `apple-touch-icon` に SVG を使えない | PNG を用意する |
| 直したのに古い画面が出る | サービスワーカーがキャッシュ優先だった | つながっていれば最新を取りにいく |
| 操作のたびに一瞬止まる | 操作ごとに `localStorage` へ同期で書き込み、全体を描き直していた | 書き込みはまとめる。描き直しは変わったぶんだけ |
| タップしても反応しない | 上に重ねた層が全面をふさいでいた | 反応させたい所だけ `pointer-events` を有効にする |
| 補助機能が途中で使えなくなる | 手もとにあるものしか探していなかった | すでに使われているものも回収の対象にする |
| 最後の1枚が押せず、終われない | 作る時に最小サイズを守っても、あとから切り抜くとその下限が壊れる | 押せない大きさになった物は、隣の押せる物に預けて一緒に片づける |
| 指を画面の端の帯へ滑らせると、撒く場所が置き去りになる | 指がキャンバスの外へ出た時点で `pointermove` が届かなくなるのに、押しっぱなしの判定だけ残り、最後に見えた位置へ出し続けていた | `pointerdown` で `setPointerCapture` して、離すまで指を捕まえておく |
| 画面の中央に出した枠が、細く縦に潰れる | `position: fixed; left: 50%` だと、幅を決める余地が画面の右半分しか残らない (`transform: translateX(-50%)` は描画位置をずらすだけで、幅の計算には効かない) | `width: max-content` と `max-width` で内容ぶんの幅を取る。`padding` を幅に含めるため `box-sizing: border-box` も要る |
| それらしい指標で難易度を決めたら、体感と逆になる | 「細い形ほど難しいはず」と形のまるさで決めたが、実測との相関は -0.22 で当たっていなかった (いちばん細い型がいちばん易しかった) | 指標を決める前に、実機で測った値との相関を見る。当たらない指標は捨て、当たるもの (落ちてきた物をどれだけ受け止められるか) に替える |
| 動きの前後を測ったら、触っていない場所まで変わっていた | 落下が終わる前に「前」を測っていた。終端速度から計算すると落ちきるまで約3秒かかるのに、2.3秒で測っていた | 測る前に、動きが終わる時間を速度から計算して待つ。「たぶん終わっている」で測らない |
| 見張りが、直したい中身ではなく見た目の数値を見ていた | 「形が裏返る」不具合の見張りを「細いこと」で書いたため、意図して太らせただけで落ちた。症状の本質は細さではなく、へこみが消えることだった | 見張りは、症状ではなく**不具合そのものの性質**に当てる (この場合は面積と凸包の比) |
| 1回の計測で調整値を決めたら、次の計測で落ちた | 同じ条件で実測が10ポイント振れていた。ぎりぎりの設定ほどよく振れる | 調整値を決める計測は複数回まわし、ばらつきごと見る。振れ幅が大きいときは「設定がぎりぎり」の合図 |
| 自動で試す操作が1通りしかなく、特定の条件だけ不当に厳しく出る | 全部の型を「横に振る」だけで試していたため、弧の形に不利な結果が出ていた。なぞる打ち方に変えたら結果が変わった | 自動で試す操作は、下手な打ち方と上手な打ち方の両方を用意する。判定には上手な方の結果を使う |
| 縦書きの題字で、漢字だけが同じ場所に重なって刷られる | `writing-mode: vertical-rl` は字の送りをフォント任せにする。手もとの環境では漢字の送りが 0 で返り、字間(letter-spacing)の 11px しか離れていなかった。箱は画面からはみ出していないので、はみ出しの見張りは通っていた | 1字ずつ箱に入れて縦に積む(`line-height` で送りを決める)。見張りは、字が何px離れて並んでいるかを1字ずつ測る |
| 背景にうっすら照りを足したら、遅い端末でカクつく | 全画面のグラデーションを毎フレーム塗っていた。CPU を4倍遅くして片方ずつ止めて測ると、照りを止めるだけで 50.0ms → 16.7ms (20fps → 60fps) だった | 動かないものは毎フレーム塗らない。敷きっぱなしで済む照りは CSS の背景へ逃がし、Canvas は動くものだけを描く |
| 道具を入れ直したら、ブラウザのテストが1件も動かなくなった | 端末名 (`iPhone 16 Plus`) を決め打ちしていて、入っている playwright がその名前を知らなかった | 同じ大きさの端末を控えに用意して、無ければそちらで代える。測り直したいときにテストが動かせないのがいちばん困る |
| 動きを測る見張りが、3回に1回だけ「1ミリも動いていない」で落ちる | ブラウザは、見えていない画面の `requestAnimationFrame` を止める。テストを画面ごとに分けていると、測りたい画面が後ろに回っていた | 測る前にその画面を前に出し(`bringToFront`)、動き出したのを確かめてから測る |
| 「消えたか」を測る見張りが、消えているのに落ちることがある | 横一列をならして明るさを比べていたが、その列に指で蒔いたものが積もっていた。積もり方しだいで 2.3〜8.8 と揺れる | 測るのは、指が触れていない側だけにする。前と後で同じ範囲を測る |
| 大きい絵をそのまま置くと、開くのが重い | 1024x1536 の PNG が 1.4MB あった | ブラウザ自身に焼き直させる(canvas から JPEG 品質0.92 で書き出し、173KB)。暗い絵でも見比べて劣化は分からなかった |
| 直したはずの不具合が、別の場所にそのまま残っていた | 同じ書き方(フォント任せの縦書き)を2か所でしていたのに、目についた方だけ直していた | 直したら、**同じ書き方をした所が他にないか探す**。見張りも、直した場所だけでなく同じ作りの場所すべてに当てる |
| 消したものが、別の場所を触った拍子に戻ってくる | 見えている絵だけ消していた。描き直しは「下地 + 記録」から組み立てる作りなので、記録が残っていると次の描き直しで戻る | 消すときは **記録から捨てる・焼き付けた下地の画素を消す・描き直す** の3つで1組にする。見張りは「消したあと別の場所を触っても戻らない」を見る |
| 触るたびに全部を描き直していて、溜まるほど重くなる | 一筆ごとに積もったもの全部を塗り直していた。2000枚で CPU4倍遅の1コマ 83ms (12fps) | **触った通り道だけを切り抜いて、そこに関わるものだけ塗り直す。** 溜まった量によらず一定になる (16.7ms / 60fps) |
| ボタンの名前を変えたら、中の絵まで消えた | `textContent` でボタンごと書き換えていた。絵と名前が入った作りに変えたあとも、書き換えはそのままだった | 名前だけを持つ入れ物を作り、そこを書き換える。見張りに「名前が変わっても絵が残る」を足す |
| 速くなぞると、跡が波のようになり、指の後ろに物が残る | 動かす物を「飛んでいる最中」の別の入れ物へ移していたため、次のひと押しが当たらず、押された場所に置き去りになっていた。しかも1回で動くのは「そのあいだに指が進んだぶん」だけで、指について来ない | 指の下に**へら(円)**があると考え、円に入った物を**円の前ふちまで押し出して、すぐ元の入れ物へ戻す**。次のひと押しでも続けて押されるので、指について来て跡が空になる。寄せた山は横へ少しずつ逃がす(逃がさないと山が育ち続け、毎回その山ぜんぶを描き直すことになる) |
| ホーム画面から開くと、画面の下に帯のような空きが出る | iOS が渡す「割合で数えたときの高さ」だけが、上の安全域ぶん足りなかった。実機で 描ける873 / %873 なのに **vh932 / 外932** | **根っこ(`html, body`)の高さを `100vh` にする。** それだけで渡される高さ自体が 932 に変わった(実測 描ける873 → 932)。全画面のものは根っこの中に `absolute` で置く。念のため、下まで見せたい絵は `html` の背景にも敷く(背景は切り取られない) |
| 効いていた直しを「届いていない」と決めつけて、取り下げてしまった | 写真が1ptも変わらなかったので「端末に届いていない」と判断した。実際は一度ぶん遅れて届いていただけで、次の写真では直っていた。**そのとき診断に版の番号が入っていなかったので、確かめずに推測で決めた** | 版の番号を**最初から**診断に入れる。番号が無いうちは「届いていない」と決めない。**使う人の操作を疑うのは最後**(先方はずっと開き直していた) |
| その空きを「足りないぶん下へずらす」で直したら、文字が切れた | ずらした先はウェブ画面の**外**。CSS では何も描けないので、はみ出したぶんが切れるだけだった。直した後の写真を測ると、いちばん下に描かれているものは 873pt のまま変わっていなかった | **直したと言う前に、同じ測り方でもう一度測る。** 見張りも「中身が渡された画面の中に収まっているか」で書く(切れていたときに落ちる見張りが無かったのが抜けだった) |
| スマホ側の表示の指定を変えたのに、何も変わらない | iOS は**ホーム画面に追加したときの設定を覚えている**。`apple-mobile-web-app-status-bar-style` を書き替えても、追加済みのアイコンから開くかぎり効かない(実機で `window.screenY` が 0 のままだった) | その手に頼らない。頼るなら、アイコンを削除して追加し直してもらう必要があると先に伝える |
| 二色を混ぜたら、色が濁って浅くなった | 色の輪で離れた二色を混ぜると、中間で色が抜ける。実測で平均の彩度 0.397 → 0.295、濃い色の割合 37% → 14% | 混ぜるのは**隣り合う色どうし**にして、**境目を狭く**する(端は色を残す)。見張りは画素の彩度ではなく、**組にした二色が色の輪でどれだけ離れているか**に当てる(彩度は引き方で 0.32〜0.39 と揺れて見張りが落ちる) |
| 色を測る見張りが、測るたびに違う値を出す | 画面そのものを測っていたので、**きらめきの白い光や落下中の箔**が混ざっていた | 測るのは「積もったものだけを写した絵」にする。動いているものが混ざらない入口を用意しておく |
| 離れた丸ふたつの型を足したら、丸と丸のあいだに線が出た | 型を「ひとつながりの点列」で持っていたので、輪と輪のあいだにも辺ができていた。塗りにも当たり判定にも、輪郭の飾りにも、無いはずの線が出る | 型は**輪(閉じた点列)の配列**で持つ。面積は足し算、内外は輪ごとの偶奇でそのまま正しく出る。輪郭をなぞる処理は「点列」ではなく**辺の列**にする(つなぎの辺が端から無い) |
| その見張りを「丸の中心どうしの中点が外か」で書いたら、正しいのに落ちた | 梅鉢(中心+五弁)では、向かい合う弁の中点は中心の丸の内側。二つ星は隙間が 2px しかなく、輪郭の金砂が届いていた | 見張りは**不具合そのものの性質**に当てる。ここでは「輪をまたぐ長い辺が無いこと」――その型の輪の中にある辺の最長と比べる(「型の大きさの1/6」で書くと、辺そのものが長い四つ目で落ちる) |
| 「輪郭から離れた所は暗いはず」の見張りが、正しいのに落ちた | 離れ具合を**点(頂点)まで**の距離で測っていた。四つ目は角が4つしかなく、角から離れていても辺の真上、という所があった | 距離は**線分まで**で測る |
| 自動で打たせた腕前が、前に測った値より大幅に悪かった | 打ち方を1通りしか用意していなかった。振り幅と高さを変えて何通りも試すと、こぼれが 46% → 7% まで下がった | 腕前を測るときは**打ち方を何通りも自動で試し、いちばん良い回を採る**。しかも「埋まり70%以上」(名人の条件)を満たした回に限る。こぼれだけ低くても、埋まらなければ意味がない |
| 測り方を揃えたら、難易度が◆3に固まって◆5が消えた | 線引き(0.30/0.45/0.58/0.70)は前の測り方の値に合わせたもの。測り直した値は 0.11〜0.62 に収まっていた | **測り直したら、線引きも引き直す。** 実測がどう散っているかを見てから決める |

| ごほうびの鍵を「記録から計算」で出したら、記録を消すと閉じた | 段位は記録から出している。鍵をそこへ直につなぐと、「記録を消す」でせっかく開いた物まで消える | **一度開いたら閉じない印を、記録とは別に持つ。** 見張りは「記録を消しても、帳の札が開いたまま」を見る(内部の値だけでなく、画面に出ている札も見る ―― 値だけ見ていると、札を記録から描いている間違いを見逃す) |
| モードを足したら、採点まわりが全部おかしくなった | 「型が出ているか」と「点が付くか」を同じ1つの旗で見ていた。下絵として型だけ出すモードを足すと、匙が減り、道具が使えず、採点が走った | **状態の意味が2つに分かれたら、旗も2つに分ける。** ここでは `odai.active`(型が出ている)と `採点中()`(点が付く)。呼び出し側を1つずつ見て、どちらの意味かを決める |

| 褒美の色を増やしたら、一覧が画面からはみ出して、最後の色が押せなくなった | 高さの上限(`max-height: 340px`)を5行ぶんで決め打ちしていた。行が8つに増えた | **数が増える一覧は、上限を決め打ちしない。** `min(400px, 58vh)` のように画面なりに詰め、あふれたら中で送る(`overflow-y: auto`) |
| 閉じている物に「あと何型で開くか」を別の行で添えたら、そこだけ背が高くなって並びが崩れた | 添え書きを `display: block` にしていた | 添え書きは**名前と同じ行**に小さく置く。行の高さを変えない |
| 勾配を持つ色を2種類目に増やしたら、どちらの表の色か分からなくなった | 箔1枚が持つのは通し番号ひとつだけ。表を2本に分けると、記録にも描き直しにも「どちらの表か」を持たせることになる | **表は1本につなぎ、色ごとに使う範囲を決める。** 見張りは「その色が自分の範囲の外を使っていないこと」を見る |

| 「まだ開いていない」物を暗くしたら、壊れているように見えた | 黒地の上で `grayscale(0.85) brightness(0.55)` + 行ごと `opacity 0.45` にしたので、ほぼ黒い丸になった。読み込みに失敗した何かに見える | 色は**沈めつつ残す**(内側に暗幕を一枚)。細い輪で縁取り、**封の印**を置いて「封じてある」と分かる形にする。見張りは明るさで測る ―― 丸の中の平均が黒地よりはっきり高く、開いている物よりは低いこと |
| その封の印が、どうやっても出てこない | 暗くするのに `filter` を使っていた。**filter は中の擬似要素まで一緒に沈める**(実測で印のいちばん明るい所が 124 までしか出なかった) | 暗幕は `box-shadow: inset` でかける。上に載る印はそのまま光る(実測 184) |

| タイトルの箔が、開いた直後ほとんど降ってこない。見張りも3回に1回落ちる | **1コマ目の `dt` が負**だった(実測 **-4.04秒**)。上限だけ `Math.min(dt, 0.05)` で止めていたので負は素通りし、歩幅が **-242コマぶん**に。その1コマで箔が全部 ±1000px 横へ飛び、「画面の外へ出た」と見なされて上へ戻される。落ちてくるまで20秒ほど空っぽの画面になる | **コマの間隔は上も下も止める。** `d < 0` なら 0 を返す。メインループの `dt` は全部の動きに掛かるので、ここ1か所の抜けが画面全体に出る |
| その見張りが「明るさ」で書いてあり、原因に当たっていなかった | 金色の画素数で見ていたため、たまたま残った数枚で通ってしまう回があった(実測 3〜2463画素と大きく振れる) | 見張りは**不具合そのものの性質**に当てる。ここでは「箔が何枚、画面の中に残っているか」(直す前 0〜7/40枚、直した後 28〜33/40枚) |
| 画面の大きさが一瞬 0 で渡ってくる | 端末の回転中や、ブラウザが枠を組み直している最中に起きる。その値で組み直すと、落ちているものが全部「画面の外」になる | 組み直す前に **`w >= 1 && h >= 1` を確かめる。** 0 のときは前の大きさのままにする |

| 新しい効果を足したら「遅い端末で2倍重い」と出て、削り続けても直らなかった | **落ち切る前に測っていた。** 3.6秒で測ると 28ms、7秒待って測ると 16.7ms。表にある「動きが終わる時間を計算して待つ」を自分で踏んだ。その間違った数字を頼りに、効果を何度も削ってしまった | 測る前に**必ず落ち切らせる**。そのうえで「遊んでいる最中」と「落ち切ったあと」を**別々に**測る。重いのはたいてい前者で、そこは新しい効果ではなく元からの描画が効いている(実測: 蒔きながら 金箔29ms・紅焼箔37ms・玉虫箔40ms) |
| 「いちばん光る」を数と大きさで作ろうとすると、遅い端末で高くつく | 加算合成の塗りは**面積**で効く。大きさ1.56倍で 29ms、等倍で 17ms | **濃さ(alpha)で稼ぐ。** 塗る面積が変わらないので値段がつかない。数を増やすなら上限を置く |
| その見張りが、作り方を変えたとたん意味を失った | 「光の量」を Σscale で数えていた。濃さで稼ぐ作りに変えたら scale はほぼ 1 になり、差が 1.5倍まで縮んで落ちた | 見張りの指標は**人が感じるもの**に合わせる。ここでは「濃さ x 面積」(Σ kira x scale²)。実測 3.5〜4.1倍に対し、線は 2.5倍に置く |

| 対象外の端末に手を広げて、余計な作りと見張りを足した | 「対象は iPhone 16 Plus だけ」と一度言われていたのに、320px 幅で溢れるのを見つけて `min(62px, 16vw)` と狭い端末の見張りを足してしまった | **決めごとは上の「決めごと」に書いて、毎回そこを見る。** 対象外の端末で見つけた不都合は、直さずに捨てる |
| 「アプリを切り替えたら一時停止するか」を試したつもりが、何も試せていなかった | ヘッドレスのブラウザは、別のタブを前に出しても `visibilityState` が `visible` のままで、`visibilitychange` も一度も来ない。処理が無い版でも有る版でも同じ結果になるのに「再現した」と報告していた | 試す前に、**その条件が本当に起きているか**を先に確かめる(ここでは `visibilityState` とイベントの回数を読む)。起きないなら `document.hidden` を差し替えてイベントを自分で送る |
| 根っこを `100vh` にしたのに、ホーム画面から開くと下の帯が消えなかった | 根っこに `position: relative` が無く、全画面の入れ物(`absolute; inset: 0`)の基準が、iOS の渡す枠(873)のままだった。上の行の「根っこの中に absolute で置く」の「中に」を読み落としていた | `html, body` に `height: 100vh` と **`position: relative` の両方**を付ける。手もとのブラウザでは差が出ないので、版の横に「描ける / 窓(innerHeight) / vh」を出し、実機の写真1枚で確かめる(直ると 描ける932 / 窓932 / vh932) |
| 丸を重ねて作った雲を薄く描いたら、丸どうしのふちが透けて輪がいくつも見えた | `globalAlpha` を下げたまま、ふち取り→塗りの順に1つずつ描いていた。塗りが半透明なので、下にある内側のふちが透ける | 一度**別の絵(オフスクリーン canvas)に不透明で描き**、その絵ごと薄めて貼る |
| 背景の幕を透明にしたら、隠れていたはずの表示(0m)が空に浮いて見えた | 暗い幕が下の表示を見えにくくしていただけで、消してはいなかった | 幕を変えたら、**その下にあるものを1つずつ見直す。** ここでは `#board:has(#vStart.on) .hud { visibility: hidden }` |
| 画面に収まったつもりが、実機では下がはみ出す | 手もとのブラウザは安全域(iPhone 16 Plus で上59・下34)が 0 なので、余白込みの高さを測っていなかった | 安全域ぶんの余白を**差し込んで**測る(`padding-top: 65px` などを一時的に足して `scrollHeight <= 932` を見る) |
| 縦横の比が画面と違う絵を敷いたら、余った所が変になった | 絵全体をぼかして余りに敷くと、絵に描いてあるボタンがぼやけて「2つ目のボタン」に見えた。絵の下端の帯を伸ばすと、ビルの縦の線がそのまま縦の筋になった。左右を多めに切ると、端に描いてある歯車やコインが画面から出た | 切るのは端の部品が残るぎりぎりまで(ここでは 452pt・左右11pt)。余りには**部品の無い所**(上端の空だけ)を伸ばすか、絵の端に**続く物**(雲)を描き足す。描いてある数字(にせのコイン)は本物を重ねて隠し、その絵が後ろに透ける画面ではぼかす |
| タイトルの背景色を変えたら、ゲーム中の空がずっとその色のままになった | canvas の背景を**単色**で指定したあと、ゲームがグラデーションに入れ直しても、Chrome が画面を描き直さなかった。指定の値(`style.background`)は正しく変わっていたので、値を読む確かめ方では気づけなかった | 背景は単色でも `linear-gradient(色, 色)` で指定し、切り替わる前後で種類を変えない。確かめるときは値ではなく**画面の画素**を読む(スクリーンショットの色) |
| 「背景透明」で頼んだ絵が、透明ではなかった | 画像を作る道具が、透明の印の**市松模様を絵として描き込んだ JPEG** を返していた(見た目は透明に見える)。緑の背景で頼んだ絵は緑のまま | 届いたら**まず四隅の画素を読む**。市松は「明るさ中くらいの灰色」、緑は「緑の強い画素」を背景とみなし、端からつながる所を抜く。囲まれた所は、緑なら抜き、灰色は残す(キャラの金具などの灰色を消さないため)。ふちは背景色が混ざるので半透明にする |

| 札の題字が白く抜けて、ほとんど読めない | 白い縁取りを `-webkit-text-stroke` で**上から**塗っていた。輪郭が重なった書体 (太い日本語の書体に多い) では、字の中の線にも縁取りがかかる | `paint-order: stroke fill` で縁取りを字の**下**に敷き、太さは倍にする。同じ書き方をした所を全部探して直す |
| 解像度の低い絵を並べたキャンバスが、遅い端末で 30fps に落ちる | 画素の倍率 2 で描いていた。止めて測ると、文字や二度描きよりも**塗る面積**が効いていた (CPU4倍遅で 1コマ 33.3ms) | 絵の解像度が低いなら、倍率を上げても細かくならない。倍率を 1.5 に抑えた (16.7ms)。効くかどうかは片方ずつ止めて測ってから決める |
| 最後の一撃で勝ったのに、勝利の画面が出ずに止まる | 「勝った」の知らせを、操作 (パンチ) の中でも出していた。画面は「戦っている最中」だけ知らせを読んでいたので、勝った瞬間から読まれなくなった | 知らせは、入れ物がある限り毎コマ読む。見張りは「最後の一撃を**操作で**入れても終わりの画面が出る」 |
| ボタンに待ち時間の輪を出したら、遅い端末で遊んでいる最中だけカクついた | `conic-gradient` の角度を CSS 変数で持ち、毎コマ細かく (50段階) 書き換えていた。変えるたびにボタンのまわりが塗り直される。CPU4倍遅で 195〜243コマ/5秒、輪を止めると 249〜255 | 段階を粗くし (12段階。目では違いが分からない)、輪を `contain: strict; will-change: transform` で自分だけの層にする (253〜255)。**層にするだけでは 203〜248 と振れて足りなかった** ―― 両方要る。見張りは「振った直後に影がかかり、明けると消える」 |
| 足した見張りが、守っているつもりの直しを外しても通っていた | 「使う人が殴られない」を直したのは2つの変更 (外したときの待ち時間・パンチ後の警戒) だったが、どちらが効いたかを測らずに、効いていない方を原因として書いていた。見張りの線 (20/30) もゆるく、片方を外しても 21〜27/30 で通った | **直しを1つずつ外して見張りを流し、落ちることを確かめる。** 外したときの実測を見てから線を引く (ここでは 28/30)。記録に書く原因も、その実測で決める |
| 画面を回す見張りが、正しい画面で落ちた | 安全域 (chromium では 0) を変数でまねていたが、横の値 (左右 59) のまま縦へ回していた。縦の幅が 430 − 118 = 312 と出た | **回したら、まねた安全域も入れ替える** (縦: 上 59・下 34 / 横: 左右 59・下 21)。実機では回すと安全域も変わる |
| 変えたあと遅くなったように見えたが、前の版も同じくらい遅かった | 前の日の計測 (253〜255コマ) と、今日の計測 (194〜250) を比べていた。同じ機械でも日によって振れ方が違う | **比べるときは、前の版 (git archive で取り出す) と今の版を同じ時に交互に測る。** 今回は12組で 前 193〜253 / 今 181〜246 と重なっていて、差とは言えなかった |
| 足した見張りが、直しを外しても通っていた (2 回目) | 「追いかけている間は攻撃してこない」を 0.9 秒だけ見ていた。敵が攻撃する間隔は 1.8 秒なので、直しを外してもその間に攻撃は起きなかった | **見張る長さは、起きるはずの出来事の間隔より長くする。** 状態を保ったまま (ここではゲージを半分より上に保って) 間隔を越えるまで見る。書いたら直しを外して落ちることを確かめる |
| 開いている札の数字が、裏で増えた値に追いつかない | 札を出したときにだけ中身を描いていた。値 (お金など) は放っておいても増えたり、別の画面で変わったりする | 札に出している値を毎コマ見て、**変わったときだけ字を書き換える** (ボタンは作り直さない。押している最中に入れ替わると押したことにならない)。見張りは「開いたまま値を変えると表示が変わり、ボタンは同じ要素のまま」 |
| 負けると何も残らず、挑み直しが同じことの繰り返しになる | 負けたら何も持ち帰れない作りだった。しかも敵の強さを自分の進み具合 (段位) で決めていると、進んでも相手も強くなるので楽にならない | **負けても何か持ち帰る** (お金など)。それで**進み具合と関係なく自分だけ強くなる**手段 (修行) を用意する。自動で「負けたら鍛えて挑み直す」を何人も遊ばせて、何回目で勝てるかを測って決める |

**新しく踏んだら、この表に足すこと。**
